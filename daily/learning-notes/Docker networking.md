- Understood **foreground processes, controlling terminal, and signal handling** (Ctrl+C behavior)

- Practiced checking whether a shell is running inside a **container vs VM** using `/proc`, `mount`, `cgroup`, and `uname`
    
- Investigated **Docker socket mounting** (`/var/run/docker.sock`) and Docker networking from host and between containers.
