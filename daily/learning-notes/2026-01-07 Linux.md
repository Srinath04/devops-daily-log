
- Deeper understanding of Linux file permissions (rwx) andSUID (Set User ID) and how executables run with the file owner’s privileges

- Explored SGID (Set Group ID) behavior on files and directories, especially for shared group collaboration.

- Understood sticky bit usage on shared directories to prevent unauthorized deletions.

- How umask control default permission values for newly created files and directories.

- Connected how security + usability are balanced using permission bits in real systems.
